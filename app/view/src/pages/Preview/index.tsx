import { PageContainer } from '@ant-design/pro-components';

export default () => {
  return (
    <PageContainer
      ghost
      header={{
        title: '预览',
      }}
    >
    </PageContainer>
  );
};
